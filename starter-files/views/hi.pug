extends layout

block content  
  - const upperName = name.toUpperCase();

  div.container
    p.text.h1 Hello #{upperName}!
    img.size-xlg(src="http://via.placeholder.com/350x150" alt=`Image of ${name}`)
